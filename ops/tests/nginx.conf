events {}

http {
    include /etc/nginx/mime.types;
    include /config/clipclap-security-http.conf;

    server {
        listen 8080;
        server_name localhost;
        include /config/clipclap-security-server.conf;
    }
}
