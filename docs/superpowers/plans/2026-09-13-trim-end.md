# Preserve a validated ending during opening trim

Evidence: identical recorded finalizer instruction on real September source s057 changes only the opening, but `tryTrim` also re-snaps and loses 2.50005 seconds of the ending validated by prior repair. Earlier experiments combined this bug fix with prompt and start-validation changes, making regressions hard to attribute.

Scope: retain existing prompt, start validation, model configuration and selection. After a successful opening snap, preserve the input end, final end node and question status; recompute duration classification. Do not restore an end that would invalidate the new interval or exceed the length cap. Full comparisons must check both extended and shortened prior ends, not assume a longer clip is better.

- [x] Add a regression test for an extended ending and confirm failure against c34dc27.
- [x] Fix only `tryTrim` in `apps/worker/src/analyze-v2/finalize.ts`; run finalizer/boundary tests.
- [x] Replay development through the full analyzer using exact-body cached upstream responses and live changed downstream requests. Track all clip boundary/identity differences.
- [x] Repeat against saved upstream responses and all existing regression sources. Inspect changed real-source endings and render affected clips. Saved repetitions vary finalizer/review answers, not independent sources; one additional fully fresh upstream run was paired too.
- [x] Run baseline-equivalent tests/build, review and document remaining release gates. Keep the isolated fix separate; production release conditions remain incomplete.
